package de.stw.phoenix.game.engine.api;

import org.junit.jupiter.api.Test;

class GameEventTest {

    @Test
    public void verifyIsComplete() {
//        final Clock clock = new ArtificialClock(5, TimeUnit.SECONDS);
//        final Moment completionMoment = clock.getMoment(20, TimeUnit.SECONDS);
//        final GameEvent gameEvent = () -> completionMoment;
//        assertThat(gameEvent.isCompleted(clock.getCurrentTick()), is(false)); // 0
//        assertThat(gameEvent.isCompleted(clock.nextTick()), is(false)); // 5
//        assertThat(gameEvent.isCompleted(clock.nextTick()), is(false)); // 10
//        assertThat(gameEvent.isCompleted(clock.nextTick()), is(false)); // 15
//        assertThat(gameEvent.isCompleted(clock.nextTick()), is(true)); // 20
//        assertThat(gameEvent.isCompleted(clock.nextTick()), is(true)); // 25
    }

}