package de.stw.phoenix.game.engine.modules.resources;

class DefaultResourceServiceTest {

    // TODO MVR implement me
//    @Test
//    public void verifyProduction() {
//        final ResourceService resourceService = new DefaultResourceService(null);
//        final ResourceProduction production = new ResourceProduction(
//                new DefaultPlayerRef(0, "Dummy"), new ImmutableResourceStorage(Resources.Iron, 0, Resources.MAX_STORAGE_CAPACITY), 3600);
//        resourceService.update(player, new Tick(0, TimeUnit.MILLISECONDS.convert(1, TimeUnit.HOURS)));
//        assertThat(production.getStorage().getAmount(), is(3600d));
//    }

}